# 车网互动充电功率预测 — 依赖清单（CPU-only，兼容 x86/ARM）
# 用法：pip install -r requirements.txt
# torch 单独装 CPU 版（见 Dockerfile 或下方注释）

pandas>=2.0
numpy>=1.24
lightgbm>=4.0
scikit-learn>=1.3
joblib>=1.3
openpyxl>=3.1

# 可选（界面/画图，训练+推理核心不需要）：
# flask>=3.0
# matplotlib>=3.7

# torch 安装（CPU 版，两个架构都有 wheel）：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
