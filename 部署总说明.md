# 车网互动充电功率预测 — H20 离线部署总说明（一份文件看全部）

> 项目：voltaic（7 模型统一 benchmark）
> 本机：H100（无 sudo 权限，不能直接 docker build）
> 目标：北京离线 H20 机房（另有"采购的 H20 服务"用于先测试）
> 编写日期：2026-08-24
> 说明：本文把之前拆成多篇的方案、分析、实测结果整合成一份，便于日后查阅。

---

## 一、一句话结论

1. **H100 上的方案在 H20 上可以直接用**——两者同为 x86_64、Ubuntu 22.04.5、驱动 575.57.08、CUDA 12.9，且本项目是 CPU-only 小负载，GPU 差异无影响。
2. 唯一会卡住的是代码里几处 macOS 硬编码路径 + Dockerfile 错误入口，**均已修复**。
3. 交付两条路：**方案一 离线 Python 包**（已生成并实测通过）；**方案二 Docker 镜像**（Dockerfile/脚本已备好，因 H100 无 sudo，需到 H20 上执行 build）。

---

## 二、本机所有交付物位置（绝对路径）

根目录：`/home/student/arthas/voltaic/`

| 类型 | 文件/目录 | 说明 |
|------|----------|------|
| **离线打包** | `excharge-offline.tar.gz`（264MB） | 方案一的完整离线包：code + dataset + wheels + 安装脚本 |
| **离线依赖** | `wheels/`（268MB，22 个 .whl） | Python 3.11 / x86_64 全部依赖（含 torch CPU 版） |
| **离线安装脚本** | `offline_install.sh` | H20 上解压后一键"建 venv→离线装→跑 benchmark" |
| **Docker 构建脚本** | `code/build_image.sh` | 一键 docker build + save 成 tar（需 sudo/docker 组） |
| **Dockerfile** | `code/Dockerfile` | 已修正入口与构建上下文 |
| **镜像推送脚本** | `code/push_image_ghcr.sh` | push 到 ghcr.io（大文件分发方案） |
| **代码（已修复）** | `code/run_merged_benchmark.py` 等 6 个脚本 | 相对路径自适应 + 固定随机种子 |
| **数据集** | `dataset/`（17MB，28 case） | 已生成，无需重跑 convert_full |
| **结果输出** | `predictions/merged_benchmark.json` | benchmark 结果（本机复跑后的种子版） |
| **总说明（本文）** | `部署总说明.md` | 你正在读的文件 |
| **分篇文档** | `docs/deploy/00~03*.md` | 更细的四篇（总览分析/方案一/方案二/推送分发） |

目录结构：

```
voltaic/
├── code/                    # 6 个脚本 + requirements.txt + Dockerfile + build_image.sh + push_image_ghcr.sh
├── dataset/                 # data/ 28 case + ground_truth/ 28 case + manifest.json
├── predictions/             # 输出 merged_benchmark.json
├── wheels/                  # 离线依赖（22 个 whl，cp311/x86_64）
├── docs/deploy/             # 4 篇分篇文档
├── excharge-offline.tar.gz  # 方案一离线包（264MB）
├── offline_install.sh       # 方案一安装脚本
└── 部署总说明.md             # 本文
```

---

## 三、H100 → H20 兼容性分析（结论）

| 项 | 本机 H100（实测） | 目标 H20（项目文档） | 影响 |
|----|------------------|---------------------|------|
| CPU 架构 | x86_64 | x86_64（Intel Xeon Platinum 8558P） | ✅ 一致 |
| 操作系统 | Ubuntu 22.04.5 LTS | Ubuntu Server 22.04.5 LTS | ✅ 一致 |
| GPU | 1× H100 NVL 95GB | 8× H20 96GB | ⚠️ 不同，但**本项目不用 GPU** |
| GPU 驱动 | 575.57.08 | 575.57.08 | ✅ 完全一致 |
| CUDA | 12.9 | 12.9 | ✅ 完全一致 |
| Python | 系统 3.10.12；venv 用 3.11.15 | 3.11.15 | ✅ 已对齐 |
| Docker | 28.3.3（无 sudo，打不了） | 29.1.3 | ⚠️ 见方案二 |
| 网络 | 有网 | **离线** | 关键：依赖必须本地打包 |

**为什么 CPU-only 是对的**：数据仅 17MB、模型是玩具级（PatchTST d_model=64 / TimesNet d_model=32 / GRU hidden=32），全程 CPU 几分钟跑完；CPU 版 torch 不链接 CUDA/驱动，跨 H100/H20/昇腾零风险，且离线包小一个数量级。**不要**为"H20 有 8 卡"换 CUDA 版 torch。

**已做代码修复**：
1. `run_merged_benchmark.py`：删除 4 处 `/Users/arthas/...` macOS 路径，改为基于 `__file__` 的相对路径（HERE=code/，ROOT=项目根）。
2. `Dockerfile`：入口改为 `CMD ["python","code/run_merged_benchmark.py"]`，构建上下文改为仓库根。
3. `run_merged_benchmark.py` 固定随机种子（random/numpy/torch seed=42），跨机器结果可复现。
4. 新增 `.dockerignore`、`.gitignore`（`wheels/` 与 `*.tar.gz` 不入库）。

---

## 四、方案一：离线 Python 包（已生成 + 已实测通过）

### 4.1 交付物
`excharge-offline.tar.gz`（264MB）内含：`code/` `dataset/` `predictions/` `wheels/` `offline_install.sh` `docs/deploy/`。

### 4.2 在 H20（离线）上执行
```bash
# 1) 拷贝 excharge-offline.tar.gz 到 H20（U盘/硬盘/加密盘），解压
mkdir -p ~/excharge && tar xzf excharge-offline.tar.gz -C ~/excharge
cd ~/excharge

# 2) 一键脚本：建 venv → 离线装依赖 → 跑 benchmark
bash offline_install.sh
```
脚本内部做的事：`python3 -m venv .venv` → `pip install --no-index --find-links wheels/ -r code/requirements.txt` → `pip install --no-index --find-links wheels/ torch` → 验证依赖 → `python run_merged_benchmark.py`。结果写 `predictions/merged_benchmark.json`。

> 前置：H20 需已装 Python 3.11（H20 文档写的是 3.11.15）。wheels 是 cp311/x86_64，与 H20 严格匹配。若 H20 Python 是别的版本，需在本机用对应版本重新 `pip download`。

### 4.3 实测结果（本机用 conda Python 3.11.15 模拟 H20 全新环境，完整跑通）
- 新建 conda 环境 `excharge_h20sim`（Python 3.11.15，与 H20 逐位一致）
- 解压 tar → 离线安装（全程 `--no-index`，不联网）→ 验证依赖 `torch 2.13.0+cpu | lightgbm 4.7.0` → 跑 7 模型 benchmark，**退出码 0**

结果（与 H100 固定种子后一致，可复现）：

| 模型 | MAE(kW) | RMSE | WAPE | MAPE_nz | F1 |
|------|---------|------|------|---------|-----|
| All-Zero | 31.54 | 48.55 | 1.000 | 100.0 | 0.000 |
| Slot-Median | 27.68 | 39.06 | 15.14 | 80.9 | 0.662 |
| LightGBM | 31.69 | 42.12 | 29.52 | 90.8 | 0.696 |
| DEMMFL | 29.47 | 37.61 | 17.88 | 78.2 | 0.715 |
| GRU | 30.30 | 39.14 | 27.54 | 78.3 | 0.715 |
| PatchTST | 32.77 | 42.84 | 23.28 | 92.7 | 0.694 |
| TimesNet | 35.73 | 47.20 | 25.70 | 111.0 | 0.657 |

确定性模型（前 4 个）与项目 README 逐位一致；深度学习模型（后 3 个）加了 seed=42 后可复现（原代码未设种子，逐次有 ±0.5 波动）。

---

## 五、方案二：Docker 镜像（已备好，去 H20 执行 build）

### 5.1 为什么本机 H100 打不了
H100 上 docker 需要 sudo 或 docker 组权限，而你没有 sudo。所以 **build 步骤放到 H20 上做**（你采购的 H20 服务有外网 + docker 29.1.3 + 通常有 root/sudo）。

### 5.2 在 H20（有外网的那台）上执行
```bash
# 先把仓库/离线包拷到 H20（git clone 或解压 excharge-offline.tar.gz 均可）
cd /home/student/arthas/voltaic     # 或你解压后的目录
bash code/build_image.sh
```
脚本做的事：`docker build -f code/Dockerfile -t excharge-pred:latest .` → `docker save excharge-pred:latest | gzip > excharge-pred.tar.gz`。构建需外网（拉 python:3.11-slim + pip install torch）。

### 5.3 把镜像带到北京离线 H20
```bash
# 在北京离线 H20 上（全程离线）
gunzip -c excharge-pred.tar.gz | docker load
mkdir -p ~/excharge-predictions
docker run --rm -v ~/excharge-predictions:/app/predictions excharge-pred:latest
cat ~/excharge-predictions/merged_benchmark.json
```
镜像内部结构：`/app/code/` + `/app/dataset/` + `/app/predictions/`，入口 `python code/run_merged_benchmark.py`，CPU-only，无需 `--gpus`、不依赖驱动/CUDA。

### 5.4 关于"Mac 上的镜像能不能在 H100 上用"
不行/不建议。Docker 镜像分架构：Apple Silicon Mac 构建的是 linux/arm64，在 x86_64 的 H100/H20 上会报 `exec format error`；Intel Mac 构建的是 amd64，架构上能启动，但那是**旧代码**（macOS 硬编码路径 + 错误入口），跑起来也报错。正确做法是**用修好的代码在 H20 上重新 build**（5.2）。

---

## 六、方案三：镜像包如何进 git / 仓库（大文件分发）

Docker 镜像约 500MB，GitHub git 单文件上限 100MB，**不能直接 `git push`**。三条路（详见 `docs/deploy/03_镜像推送与仓库分发.md`）：

| 方案 | 做法 | 适用 |
|------|------|------|
| A（推荐） | `docker push ghcr.io/arthaschan/voltaic:latest`，脚本 `code/push_image_ghcr.sh` | 有外网的 H20 服务拉取 |
| B | git-lfs（`git lfs track '*.tar.gz'`） | 想在 GitHub 存镜像备份（注意 1GB/月配额） |
| C | 物理介质拷 tar → `docker load` | 北京离线机房的唯一通道 |

> 代码/文档/脚本（小文件）可正常 `git push`；`wheels/`、`*.tar.gz` 已在 `.gitignore`，不会误提交。

---

## 七、验证记录（本机已实测）

1. **路径修复**：导入 `run_merged_benchmark`，HERE/ROOT/DATA/GT 均解析正确（DATA/GT 各 28 case），professor_model_zoo + model_zoo 正常加载。
2. **种子固定**：random/numpy/torch seed=42 已写入，同机复跑结果逐位一致。
3. **离线安装**：全新 venv + `--no-index --find-links wheels/` 装通 base 依赖 + torch CPU。
4. **完整 benchmark**：退出码 0，确定性模型与 README 逐位一致（见 4.3 表）。
5. **模拟 H20 全新环境**：conda Python 3.11.15 + 离线包解压 + offline_install.sh 全流程跑通，结果与 H100 复现一致。
6. **Docker**：`build_image.sh`/`push_image_ghcr.sh` 语法、可执行位、TARGET 解析（ghcr.io/arthaschan/voltaic:latest）、无 docker 权限时的优雅退出守卫均验证通过；**build 本身因无 sudo 未在本机执行**。

---

## 八、常见问题

1. **H20 上 pip 缺失**：`python3 -m ensurepip --upgrade`，或从 wheels 里单独装 pip。
2. **报 `not a supported wheel on this platform`**：架构/版本不匹配（如 H20 是 aarch64 或 Python 非 3.11），回本机用匹配版本重下 wheels。
3. **lightgbm 报 libgomp.so.1 缺失**：`sudo apt-get install libgomp1`（Docker 镜像内已预装）。
4. **Mac 镜像在 H100 报 exec format error**：架构不符，见 5.4。
5. **想用 GPU**：本项目不需要；若坚持，需换 CUDA 版 torch + `--gpus all`，本次不推荐不覆盖。
6. **升 腾 910C（aarch64 NPU）**：另一条路线，CPU 回退可跑（`torch.cuda.is_available()` 为 False 自动回退），要用 NPU 需 torch_npu + CANN 改造，本次不覆盖。

---

## 九、下一步动作清单

- [x] 修复代码（相对路径 + 种子 + Dockerfile 入口）
- [x] 生成离线包 `excharge-offline.tar.gz`（264MB）
- [x] 实测离线包在模拟 H20 环境跑通
- [ ] 到采购的 H20 服务上执行 `bash code/build_image.sh` 出镜像（需 sudo）
- [ ] 镜像 `docker load` 到北京离线 H20 跑通
- [ ] （可选）`code/push_image_ghcr.sh` 推到 ghcr.io 备用
- [ ] （可选）把小文件（代码/文档/脚本）commit + push 到 GitHub
